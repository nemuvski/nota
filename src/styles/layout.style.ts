import { css } from '@emotion/react'

const main = css`
  margin-top: 3rem;
`

const footer = css`
  margin: 4rem 0;
  text-align: center;
`

const styles = {
  main,
  footer,
}

export default styles
