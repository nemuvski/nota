import { css } from '@emotion/react'

const main = css`
  // FIXME: スクロールイベントの確認用なので削除すること
  height: 1000px;
`

const footer = css`
  margin: 4rem 0;
  text-align: center;
`

const styles = {
  main,
  footer,
}

export default styles
