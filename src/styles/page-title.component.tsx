import { css } from '@emotion/react'
import styled from '@emotion/styled'

const PageTitle = styled.h1(css`
  margin: 1rem 0 2rem;
  font-size: 1.6rem;
  text-align: center;
`)

export default PageTitle
