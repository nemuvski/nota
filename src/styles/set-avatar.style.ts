import { css } from '@emotion/react'

const root = css`
  display: flex;
  justify-content: center;
`

const image = css`
  width: 100%;
  max-width: 16rem;
`

const styles = {
  root,
  image,
}

export default styles
